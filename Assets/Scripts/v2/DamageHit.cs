using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DamageHit : TurretEffects
{
    // Start is called before the first frame update
    void Start()
    {
        _type = TurretType.DAMAGE;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
